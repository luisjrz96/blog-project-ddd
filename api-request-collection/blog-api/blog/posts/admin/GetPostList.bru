meta {
  name: GetPostList
  type: http
  seq: 1
}

get {
  url: http://localhost:8080/api/admin/posts?status=PUBLISHED
  body: none
  auth: bearer
}

params:query {
  status: PUBLISHED
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
