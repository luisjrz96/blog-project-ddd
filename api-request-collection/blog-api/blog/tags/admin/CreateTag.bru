meta {
  name: CreateTag
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/admin/tags
  body: json
  auth: bearer
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}

body:json {
  {
    "name": "Spring Boot Configurations"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
