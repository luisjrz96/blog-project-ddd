meta {
  name: GetTags
  type: http
  seq: 1
}

get {
  url: http://localhost:8080/api/admin/tags?status=ACTIVE
  body: none
  auth: bearer
}

params:query {
  status: ACTIVE
}

auth:bearer {
  token: {{BEARER_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
